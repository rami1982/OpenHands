# Sample Task: Add Hello World Function

Create a simple Python function in a file called `hello.py` that:
1. Defines a function named `greet(name)`
2. The function should return a greeting string: "Hello, {name}!"
3. Add a docstring to the function explaining what it does

This is a simple test task to verify the autonomous agent orchestrator is working correctly.
